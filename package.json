{
  "name": "gemini-deep-research",
  "version": "1.0.0",
  "description": "Automated Gemini Deep Research Orchestrator for Slack Integration",
  "author": "",
  "license": "ISC",
  "scripts": {
    "start": "python orchestrator.py",
    "start:concurrent": "python orchestrator_concurrent.py",
    "check:config": "python check_config.py",
    "install:all": "npm run install:playwright && npm run install:retrieve && pip install -r requirements.txt",
    "install:playwright": "cd playwright-mcp-state && npm install",
    "install:retrieve": "cd retrieve_report && npm install"
  },
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=8.0.0"
  },
  "workspaces": [
    "playwright-mcp-state",
    "retrieve_report"
  ]
}